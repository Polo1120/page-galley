
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#7843db', 
    },
    secondary: {
      main: '#fff', 
    },
    text: {
      primary: '#000',
    },
  },
});

export default theme;
